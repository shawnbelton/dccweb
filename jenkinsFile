pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'mvn clean package'
                junit '**/target/*.xml'
                archiveArtifacts artifacts: 'dcc-webapp/target/*.jar', fingerprint: true
            }
        }
    }
}
